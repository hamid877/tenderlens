fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-multipart>=0.0.9,<1.0.0  # required by FastAPI for UploadFile / Form
httpx>=0.28.0,<1.0.0   # application HTTP client
httpx2>=2.10.0,<3.0.0  # required by Starlette 1.x TestClient
pytest>=8.2.0,<9.0.0
pypdf>=4.0.0,<5.0.0    # PDF text extraction (Milestone 2)
# Milestone 4: embedding + FAISS stack
# sentence-transformers 6.x pulls transformers 5.x which dropped is_offline_mode
# from huggingface-hub; pin the proven-compatible 3.x combination.
sentence-transformers>=3.0.0,<4.0.0   # all-MiniLM-L6-v2 embeddings
transformers>=4.41.0,<5.0.0           # must stay <5 to remain compatible with hf-hub <0.27
huggingface-hub>=0.23.0,<0.27.0       # 0.27+ removed is_offline_mode used by transformers <5
faiss-cpu>=1.8.0,<2.0.0               # FAISS vector index
